let addName = (firstName, lastName) => firstName + " " + lastName;








//Write an arrow function with the variable name addName,
 //that takes two parameters named:
  //firstName and lastName, then concatenates those two names together with a space in between.

